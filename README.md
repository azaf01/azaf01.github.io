# LiveDeck

Peer-to-peer live presentation sharing — host uploads slides, viewers join via 5-character code or QR.

Pure static HTML/JS. No backend. Works on GitHub Pages.

- WebRTC via PeerJS for P2P transport
- PDF.js for slide rendering
- QRCode.js for join link

## Run

Open `index.html` locally, or visit the GitHub Pages URL.